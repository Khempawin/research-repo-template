# Shared Service Contracts

Shared schemas and job states for future dynamic services.

Keep cross-service contracts here when the project grows beyond a static demo. Examples:

- Review request and response schemas.
- Job status values.
- Prompt contract versions.
- Model/provider routing metadata.

If the implementation language differs across services, prefer language-neutral schemas such as
OpenAPI, JSON Schema, or Protocol Buffers.

